# ============================================
# Frontegg Next.js SDK - Client-Side Config
# ============================================
# These variables are exposed to the browser (NEXT_PUBLIC_ prefix)

# Your Frontegg environment's Client ID (required for SDK)
NEXT_PUBLIC_FRONTEGG_CLIENT_ID=your_frontegg_client_id

# Your Frontegg API Key (required for vendor token and admin operations)
NEXT_PUBLIC_FRONTEGG_API_KEY=your_frontegg_api_key

# Your Frontegg base URL (tenant URL, e.g., https://your-tenant.frontegg.com)
NEXT_PUBLIC_FRONTEGG_BASE_URL=https://your-tenant.frontegg.com

# ============================================
# Frontegg Next.js SDK - Server-Side Config
# ============================================
# These variables are only available on the server

# The AppUrl you set during integration - tells Frontegg your application hostname
# For local development: http://localhost:3000
# For production: https://your-domain.com
FRONTEGG_APP_URL=http://localhost:3000

# The Frontegg domain - your unique URL to connect to the Frontegg gateway
FRONTEGG_BASE_URL=https://your-tenant.frontegg.com

# Your Frontegg environment's Client ID (server-side)
FRONTEGG_CLIENT_ID=your_frontegg_client_id

# Your Frontegg application ID (server-side)
FRONTEGG_APP_ID=your_frontegg_application_id

# The stateless session encryption password
# Used to encrypt JWT before sending it to the client side
# Generate a secure password using:
# node -e "console.log(crypto.randomBytes(32).toString('hex'))"
FRONTEGG_ENCRYPTION_PASSWORD=your_frontegg_encryption_password

# The stateless session cookie name (you should not change this)
FRONTEGG_COOKIE_NAME=fe_session

# Enable hosted login page (set to true to use Frontegg's hosted login)
FRONTEGG_HOSTED_LOGIN=true

# ============================================
# Login Page Customization
# ============================================

# URL to your login overrides endpoint (for hosted login customization)
# For local development: http://localhost:3000/api/frontegg-login-overrides
# For production: https://your-domain.com/api/frontegg-login-overrides
FRONTEGG_OVERRIDES_URL=http://localhost:3000/api/frontegg-login-overrides

# Your Frontegg application ID (required for login overrides)
# Available from @frontegg/nextjs@9.2.0
NEXT_PUBLIC_FRONTEGG_APP_ID=your_frontegg_application_id

# Your Frontegg tenant ID (required for login overrides)
# Available from @frontegg/nextjs@9.2.0
NEXT_PUBLIC_FRONTEGG_TENANT_ID=your_frontegg_tenant_id

# Your Frontegg default role ID (required for login overrides)
# Available from @frontegg/nextjs@9.2.0
NEXT_PUBLIC_FRONTEGG_DEFAULT_ROLE_ID=your_frontegg_default_role_id

# ============================================
# Optional Configuration
# ============================================

# For printing verbose log messages in regards to nextjs middleware activity
# Uncomment to enable debug logging
# FRONTEGG_LOG_LEVEL=debug

# For improving nextjs performance by providing environment public key to the nextjs middleware
# The values can be found under [ENVIRONMENT] → Authentication → Identity provider → OIDC endpoints → JSON web key
# Extract the object from within the `keys` array and use it as the below variable
# Uncomment and set if you want to improve performance
# FRONTEGG_JWT_PUBLIC_KEY='{"kty":"RSA", "kid":"xxx", "use":"sig", "alg":"RS256", "n":"xxxx", "e":"xxx"}'
